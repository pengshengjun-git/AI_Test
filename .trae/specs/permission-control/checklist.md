# Checklist - 权限控制系统验收清单

## 后端实现

### 登录接口扩展
- [x] 登录响应包含 roles 字段
- [x] 登录响应包含 permissions 字段
- [x] 登录响应包含 departmentId 字段

### 权限注解与拦截器
- [x] @RequirePermission 注解已创建
- [x] 权限拦截器正确拦截无权限请求
- [x] 拦截器返回标准的 403 响应

### 数据范围过滤
- [x] UserService.queryUsers() 根据部门ID过滤数据
- [x] 普通用户只能看到本部门及子部门用户
- [x] 管理员可以看到所有用户

### 管理接口权限保护
- [x] /api/users 接口需要 system:user:* 权限
- [x] /api/roles 接口需要 system:role:* 权限
- [x] /api/departments 接口需要 system:department:* 权限
- [x] /api/menus 接口需要 system:menu:* 权限
- [x] 非管理员调用管理接口返回 403

## 前端实现

### 用户状态管理
- [x] 登录后正确存储用户角色信息
- [x] 存储包含 roles 和 permissions 字段
- [x] hasPermission() 函数正确判断权限
- [x] isAdmin() 函数正确判断管理员

### 菜单权限控制
- [x] 管理员登录显示"系统管理"菜单
- [x] 普通用户登录不显示"系统管理"菜单
- [x] 菜单权限控制实时生效（刷新后）

### 按钮级权限控制
- [x] v-permission 指令已创建
- [x] 用户管理页面的"编辑"按钮根据权限显示
- [x] 用户管理页面的"删除"按钮根据权限显示
- [x] 用户管理页面的"禁用"按钮根据权限显示

### API权限拦截
- [x] 403 响应统一处理
- [x] 显示"无权限访问"提示
- [x] 不暴露敏感错误信息

## 功能测试

### 管理员功能验证
- [x] 管理员可以访问用户管理页面
- [x] 管理员可以创建/编辑/删除用户
- [x] 管理员可以访问角色管理页面
- [x] 管理员可以访问部门管理页面
- [x] 管理员可以访问菜单管理页面

### 普通用户功能验证
- [ ] 普通用户不能看到系统管理菜单
- [ ] 普通用户只能看到本部门数据
- [ ] 普通用户调用管理接口返回 403
- [ ] 普通用户界面上不显示管理相关按钮

## 边界情况处理
- [ ] 用户无角色时默认无管理权限
- [ ] 用户有多个角色时权限叠加
- [ ] Token过期时正确跳转登录页
